[build-system]
requires = ["setuptools>=61.2"]
build-backend = "setuptools.build_meta"

[project]
name = "sc-seminar-23-11-2023"
version = "0.1.0"
dependencies = ["jupyter-book", "jupytext", "pre-commit"]

[tool.setuptools]
packages = []
